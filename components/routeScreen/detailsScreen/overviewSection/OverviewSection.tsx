import React from "react";
import { Text, View } from "react-native";
import styles from "./overviewSection.style";

const OverviewSection = () => {
  return (
    <View style={styles.overviewSection}>
      <Text>overview</Text>
    </View>
  );
};

export default OverviewSection;
