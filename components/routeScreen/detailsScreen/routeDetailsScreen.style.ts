import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  routeDetailsScreen: {
    paddingTop: 20,
    paddingBottom:40,
    paddingHorizontal: "5%",
  },
});

export default styles;
